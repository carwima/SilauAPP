<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.silauapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TransaksiTambah"></activity>
        <activity android:name=".DetailTransaksi" />
        <activity android:name=".PaketEdit" />
        <activity android:name=".PaketTambah" />
        <activity android:name=".DetailPaket" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TransactionActivity" />
        <activity android:name=".PelangganTambah" />
        <activity android:name=".PelangganEdit" />
        <activity android:name=".ListPelanggan" />
    </application>

</manifest>